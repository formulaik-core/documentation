import FAQStructuredData from '@site/src/components/generic/faq'

# Troubleshooting 🐞


### I can't run the linked cli
#### Situation
After you've linked the CLI locally with ```yarn link``` you get a permission error.
#### Solution
```bash
chmod +x ./src/app.js
```
#### Explanation
When you link the app, the bin file is app.js. 