---
sidebar_position: 3
---

import JSONSchemaViewer from "@theme/JSONSchemaViewer"

# Command

- A command is declared by adding a file to the src/commands folder
- The tree of commands is automatically generated following the folders recursively
- Any file within a command folder is a subcommand of the command declared in the index.js file of said command

## Arguments

<JSONSchemaViewer schema={{
    	"title": "Command schema",
    	"type": "object",
        "description": "Allows for schema to be seen",
    	"properties": {
    		"_formulaikType": {
    			"type": "string",
                "default": "command",
                "enum":[
                    "command",
                ],
    		},
            "name": {
    			"type": "string",
                "description": "Unique ID"
    		},
            "description": {
    			"type": "string",
                "description": "Command description"
    		},
    		"questions": {
    			"type": "array",                
                "description":"Command questions that will be turned into CLI options"
    		},
            "example": {
    			"type": "string",
                "description": "$0 feature eject",
    		},
            "template": {
    			"type": "string"
    		},
            "handler": {
    			"type": "object",
                "description":"The command handler"
    		}
    	},
    	"required": ["name",]
    }} />
